<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

	</context:component-scan>





<!-- 파일 업로드 처리용 Multipart Resolver -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="defaultEncoding" value="UTF-8"/>
		<property name="maxUploadSize" value="52428800"/> <!-- 50MB -->
		<property name="maxInMemorySize" value="1048576"/> <!-- 1MB -->
	</bean>

	

	<bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean"/>

	<bean class="org.springframework.web.servlet.view.UrlBasedViewResolver" p:order="1"
		p:viewClass="org.springframework.web.servlet.view.JstlView"
		p:prefix="/WEB-INF/jsp/" p:suffix=".jsp"/>

	<!-- Interceptor 설정 (로그인 및 공통 파라미터 opType 주입) -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/**/*.ajax"/>
			<mvc:mapping path="/**/*.do"/>
			<mvc:exclude-mapping path="/login.do"/>
			<mvc:exclude-mapping path="/resources/**"/>
			<bean class="kr.co.matpam.common.interceptor.MatpamAuthInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>

	<mvc:annotation-driven validator="validator" />

	<!-- AuthInterceptor 등록 (데이터 격리 및 인증 관리용) -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/admin/**/*.do" />
			<mvc:exclude-mapping path="/admin/loginForm.do" />
			<mvc:exclude-mapping path="/admin/login.do" />
			<bean class="kr.co.matpam.admin.common.interceptor.AuthInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>

	<!-- FrontInterceptor 등록 (소비자 쇼핑몰 세션 관리) -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/front/**" />
			<mvc:exclude-mapping path="/front/login.do" />
			<mvc:exclude-mapping path="/front/login.ajax" />
			<bean class="kr.co.matpam.admin.common.interceptor.FrontInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>

</beans>
