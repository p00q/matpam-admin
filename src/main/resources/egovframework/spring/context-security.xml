<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- 정적 자원은 보안 필터 제외 -->
	<security:http pattern="/css/**" security="none"/>
	<security:http pattern="/js/**" security="none"/>
	<security:http pattern="/images/**" security="none"/>
    <security:http pattern="/favicon.ico" security="none"/>

	<security:http auto-config="true" use-expressions="true">
		<security:csrf disabled="true"/>
		
		<security:intercept-url pattern="/admin/login.do" access="permitAll"/>
		<security:intercept-url pattern="/admin/loginProcess.do" access="permitAll"/>
		<security:intercept-url pattern="/admin/**" access="hasAnyRole('ROLE_SUPER', 'ROLE_SUPER_ADMIN', 'ROLE_SELLER', 'ROLE_BUYER', 'ROLE_CHANNEL')"/>
		
		<security:form-login login-page="/admin/login.do"
							 login-processing-url="/admin/loginProcess.do"
							 username-parameter="loginId"
							 password-parameter="loginPw"
							 authentication-success-handler-ref="customLoginSuccessHandler"
							 authentication-failure-url="/admin/login.do?error=true"
							 default-target-url="/admin/user/userList.do"
							 always-use-default-target="true"/>
		
		<security:logout logout-url="/admin/logout.do"
						 logout-success-url="/admin/login.do"
						 invalidate-session="true"
						 delete-cookies="JSESSIONID"/>
                         
        <security:access-denied-handler error-page="/admin/login.do?denied=true"/>
	</security:http>

	<security:authentication-manager>
		<security:authentication-provider user-service-ref="customUserDetailsService">
			<security:password-encoder ref="bcryptPasswordEncoder"/>
		</security:authentication-provider>
	</security:authentication-manager>

	<bean id="bcryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
    
    <!-- 로그인 성공 핸들러 (세션 LoginVO 바인딩용) -->
    <bean id="customLoginSuccessHandler" class="kr.co.matpam.common.security.CustomLoginSuccessHandler"/>

</beans>
